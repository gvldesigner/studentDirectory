//A collection of student models --------------------------------------------
 /*model constructor by calling .extend*/
 /*new is the keyword the instance of the model*/

var Students = Backbone.Collection.extend({
  model: Student
});